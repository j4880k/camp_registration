<%= form_for(@event) do |f| %>
<!-- THIS IS THE FIELD FORM FOR EVENTS -->
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<!-- fieldset><legend><b>Available Cabins</b></legend -->	
	
	<!-- %= f.fields_for :resources do |builder| % -->
		<!-- %= render "resource_fields", f: builder % -->
	<!-- % end % -->
	<!-- %= link_to_add_fields "Add Resource", f, :resources % -->	
<!-- /fieldset -->
  <div class="field">
    <%= f.label :eventname %><br />
    <%= f.text_field :eventname, :size=>100 %>
  </div>
  <div class="field">
    <%= f.label :date_begin %><br />
    <%= f.datetime_select :date_begin %>
  </div>
  <div class="field">
    <%= f.label :date_end %><br />
    <%= f.datetime_select :date_end %>
  </div>

  <div class="field">
    <%= f.label :price %><br />
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :notes %><br />
    <%= f.text_area :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
